package Week1;

public class Test5 {
  public int solution(String s) {

    int answer = 0;
    int count = s.length();

    // 입력문자열의 길이가 1일때,
    if (count == 1) {
      return 1;
    }

    // 완전 탐색으로 가장 작은 길이를 결과를 구하되, 탐색의 범위를 줄이는 것이 필요함
    // 최대 압축길이는 : 문자열 길이/2

  }
}
